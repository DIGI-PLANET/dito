node_modules/
.next/
# Private keys — NEVER commit
.env.keys
.env.local
.env.local.bak

# Encrypted env files — safe to commit (public key only)
# .env.development, .env.test, .env.production are encrypted by dotenvx
*.log
.vercel

# Figma design cache (re-fetchable via REST API)
.figma-cache/

# Redesign working state (local tracker)
.redesign-state.json

# TS build info (auto-generated)
tsconfig.tsbuildinfo
.env*.local
